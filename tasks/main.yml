---
# tasks file for ansible-virtualization
- name: Disable dnsmaq
  ansible.builtin.systemd:
    name: dnsmasq
    state: stopped
    enabled: no
  become: yes

- name: Requirements
  ansible.builtin.include_tasks: requirements.yml
  when:
   - standalone_role is defined
   - standalone_role
  tags:
    - standalone

- name: Configure users
  ansible.builtin.include_tasks: users.yml

- name: Permanently allow traffic forwarding on host nic
  ansible.posix.sysctl:
    name: net.ipv4.ip_forward
    value: '1'
    sysctl_set: yes
    state: present
    reload: yes
  become: yes

- name: Permanently allow traffic forwarding on host nic
  ansible.posix.sysctl:
    name: net.ipv6.conf.all.forwarding
    value: '1'
    sysctl_set: yes
    state: present
    reload: yes
  become: yes
