exclude_paths:
 - .cache/
 - .tox/
 - meta/
 - molecule/
 - tests/
skip_list:
 - '106'  # Role name {} does not match ``^[a-z][a-z0-9_]+$`` pattern
 - var-naming[no-role-prefix]  # Variable names should not contain role name
